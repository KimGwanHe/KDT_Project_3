{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": false, // true인데 타입 검사 강도를 낮추기 위해 바꿈
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"]
    },
    "esModuleInterop": true, // ES 모듈과 CommonJS 모듈 간의 호환성을 보장합니다.
    "skipLibCheck": true, // 라이브러리 체크를 건너뛰어 빌드 속도를 높입니다.
    "jsx": "react-native", // JSX 처리를 React Native에 맞게 설정합니다.
    "lib": ["ESNext", "DOM"], // 최신 ECMAScript 및 DOM 라이브러리를 포함합니다.
    "allowJs": true, // JS 파일을 허용합니다 (이미 있는 경우).
    "noEmit": true, // 빌드 중 출력을 하지 않습니다.
    "resolveJsonModule": true, // JSON 모듈을 직접 가져올 수 있습니다.
    "isolatedModules": true // 개별 모듈 처리 활성화.
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts",
    "types.d.ts"
  ],
  "exclude": ["node_modules"]
}